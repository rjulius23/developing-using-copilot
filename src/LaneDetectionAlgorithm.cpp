#include "LaneDetectionAlgorithm.hpp"

double LaneDetectionAlgorithm::detectLaneOffset(const std::vector<int>& sensorData) {
    std::cout << "Base lane detection algorithm processing sensor data." << std::endl;
    return 0.0;
}
